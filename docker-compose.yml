version: '3.8'

services:
  postgres:
    container_name: courses-api-rs_c
    image: postgres
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: courses-api_db